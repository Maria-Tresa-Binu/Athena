import type { Transport } from "./transport.js";
import type { ConnectedAccount, ConnectionRequestStatus, WaitForConnectionOptions } from "./types.js";
export interface ConnectionRequestSnapshot {
    id: string;
    redirectUrl: string;
    status: ConnectionRequestStatus;
    expiresAt: string;
    connectedAccount?: ConnectedAccount;
    failure?: {
        code: string;
        message: string;
    };
}
export declare class ConnectionRequest {
    private readonly transport;
    private snapshot;
    readonly id: string;
    readonly redirectUrl: string;
    readonly expiresAt: string;
    status: ConnectionRequestStatus;
    constructor(transport: Transport, snapshot: ConnectionRequestSnapshot);
    refresh(signal?: AbortSignal): Promise<ConnectionRequestSnapshot>;
    waitForConnection(options?: WaitForConnectionOptions): Promise<ConnectedAccount>;
}
//# sourceMappingURL=connection-request.d.ts.map