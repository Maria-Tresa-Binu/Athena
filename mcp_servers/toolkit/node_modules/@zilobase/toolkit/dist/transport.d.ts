import type { FetchLike, RequestOptions } from "./types.js";
interface RequestConfig extends RequestOptions {
    method?: "GET" | "POST" | "PATCH" | "DELETE";
    body?: unknown;
    query?: Record<string, string | number | undefined>;
    headers?: Record<string, string>;
}
export interface DetailedResponse<T> {
    data?: T;
    status: number;
    headers: Headers;
}
export declare class Transport {
    private readonly endpoint;
    private readonly apiKey;
    private readonly timeoutMs;
    private readonly fetch;
    constructor(endpoint: string, apiKey: string, fetchImplementation: FetchLike | undefined, timeoutMs: number);
    request<T>(path: string, config?: RequestConfig): Promise<T>;
    requestDetailed<T>(path: string, config?: RequestConfig): Promise<DetailedResponse<T>>;
}
export {};
//# sourceMappingURL=transport.d.ts.map