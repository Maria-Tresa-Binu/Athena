export const TOOLKIT_TOOL_METADATA_KEY = "zilobaseToolkit";
function isRecord(value) {
    return typeof value === "object" && value !== null && !Array.isArray(value);
}
function isNonEmptyString(value) {
    return typeof value === "string" && value.trim().length > 0;
}
export function getToolkitToolMetadata(toolMetadata) {
    if (!isRecord(toolMetadata))
        return undefined;
    const candidate = toolMetadata[TOOLKIT_TOOL_METADATA_KEY];
    if (!isRecord(candidate) || candidate.schemaVersion !== 1)
        return undefined;
    if (candidate.access !== "read" && candidate.access !== "write") {
        return undefined;
    }
    if (!isRecord(candidate.annotations) ||
        ![
            candidate.annotations.destructiveHint,
            candidate.annotations.idempotentHint,
            candidate.annotations.openWorldHint,
            candidate.annotations.readOnlyHint,
        ].every((value) => typeof value === "boolean")) {
        return undefined;
    }
    if (!isNonEmptyString(candidate.connectorId) ||
        !isNonEmptyString(candidate.toolId)) {
        return undefined;
    }
    if (!isRecord(candidate.presentation))
        return undefined;
    if (!isNonEmptyString(candidate.presentation.title))
        return undefined;
    const progressPhrases = candidate.presentation.progressPhrases;
    if (!Array.isArray(progressPhrases) ||
        progressPhrases.length === 0 ||
        !progressPhrases.every(isNonEmptyString)) {
        return undefined;
    }
    return candidate;
}
//# sourceMappingURL=metadata.js.map